# Project & component variables
PROJECT = masterproef
COMPONENT = pytorchtutorials

# Container run variables
USER = jefp
CONTAINER_INSTANCE = default

# Overridable parameters
DATA ?= $(PWD)/data
WORKSPACE ?= $(PWD)/src

# Container options
VOLUMES = -v $(DATA):/data -v $(WORKSPACE):/workspace --gpus all

